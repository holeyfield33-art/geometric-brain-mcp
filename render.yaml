services:
  - type: web
    name: geometric-brain
    runtime: python
    buildCommand: pip install --upgrade pip && pip install -r requirements-ci.txt
    startCommand: python api.py
    healthCheckPath: /healthz
    envVars:
      - key: PORT
        value: 10000
    plan: free
